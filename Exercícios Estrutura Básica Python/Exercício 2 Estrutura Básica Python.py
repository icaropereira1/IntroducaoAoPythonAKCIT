distance = float(input('What is the distance the vehicle traveled in km? '))
fuel = float(input('How much fuel has it spent in liters? '))

average_fuel_per_distance = distance / fuel

print('The average fuel per distance is:',average_fuel_per_distance,'km/l')